<%- include('../includes/header'); %>

  <div class="container">
    <h2>
      Tambah Pesanan
    </h2>
    <form action="/pesanan/store" method="POST">
      <div class="mb-3">
        <label for="id_menu" class="form-label">ID Menu</label>
        <select class="form-control" id="id_menu" name="id_menu">
          <% menu.forEach(e => { %>
            <option value="<%= e.id_menu %>"><%= e.nama_menu %></option>
          <% }) %>
        </select>
      </div>

     <div class="mb-3">
        <label for="id_kasir" class="form-label">ID Kasir</label>
        <select class="form-control" name="id_kasir">
          <% kasir.forEach(e => { %>
            <option value="<%= e.id %>"><%= e.name_kasir %></option>
          <% }) %>
        </select>
      </div>
      <div class="mb-3">
        <label for="harga" class="form-label">Harga</label>
        <textarea type="text" name="harga" class="form-control" id="harga"></textarea>
     </div>
      <div class="mb-3">
        <label for="nomor_meja" class="form-label">Nomor Meja</label>
        <textarea type="text" name="nomor_meja" class="form-control" id="nomor_meja"></textarea>
     </div>
      <div class="mb-3">
        <label for="qty" class="form-label">Qty</label>
        <textarea type="text" name="qty" class="form-control" id="qty"></textarea>
     </div>
      <div class="mb-3">
        <label for="inputStatus">Status</label>
        <select class="form-control" name="status" id="inputStatus" >
          <option value="Belum bayar">Belum bayar</option>
          <option value="Dibayar">Dibayar</option>
          <option value="Selesai">Selesai</option>
        </select>

     </div>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </div>

  <%- include('../includes/footer'); %>